# Copy to `.env` and fill in your key. The .env file is gitignored.
# Required: OpenRouter API key used by the harness LLM (src/adapters/open_router.py).
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=
